version: 1
send_anonymous_usage_stats: true
project_id: "tap-bing-ads"
default_environment: test
venv:
  backend: uv
environments:
- name: test
plugins:
  extractors:
  - name: "tap-bing-ads"
    namespace: "tap_bing_ads"
    pip_url: -e .
    capabilities:
    - state
    - catalog
    - discover
    - about
    - stream-maps

    settings:
    - name: client_id
      label: Client ID
      description: App OAuth 2.0 client ID

    - name: client_secret
      label: Client secret
      description: App OAuth 2.0 client secret
      sensitive: true

    - name: refresh_token
      label: Refresh Token
      description: App OAuth 2.0 refresh token
      sensitive: true

    - name: start_date
      kind: date_iso8601
      label: Start Date
      description: Initial date to start extracting data from

    settings_group_validation:
    - [client_id, client_secret, refresh_token]

    # TODO: Declare default configuration values here:
    config:
      start_date: '2010-01-01T00:00:00Z'

  loaders:
  - name: target-jsonl
    variant: andyh1203
    pip_url: target-jsonl
